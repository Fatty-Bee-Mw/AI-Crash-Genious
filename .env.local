# Admin universal code (plaintext)
ADMIN_CODE=YusufLikagwa@Password

# Optional: SHA-256 hex of the admin code (not required since UI sends plaintext header too)
# ADMIN_CODE_HASH=

# Comma-separated WhatsApp recipients (digits only or local starting with 0)
NEXT_PUBLIC_WHATSAPP_RECIPIENTS=0880646248
